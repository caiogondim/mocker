# Reduce build context size (~5GB without these exclusions)
.git
node_modules/
experiments/
proxies/

# Everything else excluded by selective COPY in Dockerfile
# (only mocker, package.json, src/ are copied)
