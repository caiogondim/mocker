Simple HTTP server to help development of mocker.

It returns the status code on response passed as query param `?statusCode=200`

### Usage
```bash
node index.js 1337 # 1337 is the port to be used
```